# DQR.GG 攻略站 — 设计规范（交付 Codex 实现）

> 对应效果图：`mockups/final.html`（首页）。技术栈：Next.js + Tailwind + shadcn（沿用"游戏站脚手架-main"）。
> 网站内容全部英文；本文档为实现规范。方案定稿：**C 速查结构 + A 配色 token + B 门框 Logo Hero**。

---

## 1. 设计 Token

### 1.1 色彩（写入 tailwind config / CSS variables）

```css
:root {
  --background:      #0B1020;  /* 页面底 */
  --surface:         #131B2E;  /* 卡片/面板 */
  --surface-hover:   #19243C;  /* 悬停态 */
  --border:          #293653;  /* 全部描边 */

  --text-primary:    #F3F6FC;
  --text-secondary:  #AAB6CC;

  --accent-gold:     #F2C14E;  /* 主强调：Logo、TLDR、代码、S 级 */
  --accent-blue:     #68C8FF;  /* 链接、版本标签、Logo 内门 */
  --accent-purple:   #A87BFF;  /* A 级、宝石装饰 */

  --success:         #55D68B;  /* 新鲜度圆点、ACTIVE */
  --warning:         #FFB454;
  --danger:          #FF6B6B;
}
```

### 1.2 稀有度色阶（装备 + Tier 分级共用同一套）

| 档位 | 颜色 | 用途 |
|---|---|---|
| Common / D | `#8A93A6` 灰 | |
| Uncommon / C | `#55D68B` 绿 | |
| Rare / B | `#5EB8FF` 蓝 | |
| Epic / A | `#A87BFF` 紫 | |
| Legendary / S | `#F2C14E` 金 | |
| Ultimate | `#FF5C5C` 红 | |

**硬规则：稀有度只用「边框 + 菱形小标 + 文字标签」表达，禁止整卡染成高饱和色。** Epic+ 卡片允许极弱的外发光（`box-shadow: 0 0 16px color/15%`），Common–Rare 不允许任何光效。

### 1.3 字体

- 标题（H1/H2、Logo、Tier 字母、地牢序号）：**Cinzel**（Google Fonts，仅 600/700）
- 正文 / UI / 表格 / 卡片标题（H3 及以下）：**Inter**（400–800）
- 代码 / 兑换码：ui-monospace / Cascadia Code
- 幻想字体**只**出现在：Logo、H1、H2 区块标题、Tier 等级字母。正文必须保持高可读性。

### 1.4 圆角与间距

- 卡片圆角 10–12px，磁贴/芯片 7–10px，徽章 999px
- 区块间距：区块标题上间距 36px，卡片间隙 10–14px，页面左右 padding 18px（移动端同）
- 容器最大宽度 1060px

---

## 2. 页面结构（首页，自上而下）

> 效果图每个区块在 HTML 里都有对应注释锚点，Codex 可直接对照。

| # | 区块 | 说明 | 关键交互 |
|---|---|---|---|
| 1 | 顶部导航（sticky） | Logo（门框 SVG）+ 7 个栏目 + 常驻搜索框（带 `Ctrl K` 提示） | 搜索为全站 Instant Search 入口（Fuse.js），移动端收缩为图标 |
| 2 | Hero | 门框 Logo 大图 + 版本胶囊（❄ NORTHERN LANDS）+ H1 + 一句定位 + 「Last verified」新鲜度圆点 | 无按钮，克制氛围；禁止全屏海报 |
| 3 | TL;DR 答案卡 | 金色虚边框大卡：当前版本结论 + 3 个"问→答"格子 | SEO 核心：首屏直接给出答案（Featured Snippet 目标） |
| 4 | 热词磁贴 ×6 | Tier List / Codes / Drop Tables / Trading / Spell Guide / All Dungeons | 对应搜索下拉词，点击直达 |
| 5 | Meta Tier List | S/A/B 三行：左 Cinzel 大字母（渐变底），右物品芯片（稀有度边框） | 芯片点击跳词条页（内链）；表头带「● Fresh · re-ranked 日期」 |
| 6 | Dungeon Ladder | 双列地牢卡：序号 + 名称 + 核心掉落 + 等级徽章 | 最新版本地牢带 `NEW` 金徽章；点击进地牢详情页 |
| 7 | Active Codes | 虚线卡片：等宽字体兑换码 + ACTIVE 状态 + Copy 按钮 | 一键复制剪贴板，复制后按钮变「Copied ✓」 |
| 8 | Update Log | 更新日志流：日期 + NEW/DATA/FIX 徽章 + 一句话 | 信任信号；每页头部复用「Last updated · v[版本]」徽章 |
| 9 | 页脚 | 站内链接 + 免责声明（见 §6） | |

**全局原则**：答案前置、表格/卡片优先于长文、无弹窗、无悬浮广告、无自动播放。

---

## 3. 组件清单（shadcn 颗粒度）

| 组件 | 基于 | 备注 |
|---|---|---|
| `SiteHeader` | 自建 | sticky + backdrop-blur；含 `SearchBox` |
| `SearchBox` | shadcn `Command` | Ctrl K 唤起 Command Palette，Fuse.js 前端索引 |
| `VersionPill` | shadcn `Badge` | Hero 版本胶囊 + 每页「Last updated」徽章 |
| `TldrCard` | shadcn `Card` | 金边框变体，内含 3 个 `AnswerCell` |
| `HotTiles` | 自建 grid | 6 磁贴，图标用 emoji 占位（后续可换 Lucide） |
| `TierBoard` / `TierRow` | 自建 | 左字母块（`tier-s/a/b/c/d` 渐变），右 `ItemChip` 列表 |
| `ItemChip` | shadcn `Badge` 变体 | 稀有度边框 class：`rarity-ultimate/legendary/epic/rare/uncommon/common` + 菱形色点 |
| `DungeonCard` | shadcn `Card` | 序号（Cinzel）+ 标题 + 掉落摘要 + `LevelBadge` |
| `CodeCard` | 自建 | 虚线边框 + 等宽码 + `CopyButton`（navigator.clipboard） |
| `UpdateLog` / `LogItem` | 自建 | `log-new / log-fix / log-data` 徽章 |
| `DataTable`（内页） | shadcn `Table` + TanStack Table | 掉落表/武器表：可排序可筛选，移动端横向滚动 `overflow-x-auto` |
| `SiteFooter` | 自建 | 含免责声明 |

---

## 4. 响应式规则（移动优先）

- 断点：880px 一档即可（移动端 → 桌面端）；不设中间断点
- ≤880px：导航栏目隐藏（汉堡或底部 Tab 二选一，建议底部 Tab）；搜索框缩至 160px/图标；TLDR 问答格 3→1 列；磁贴 6→3 列；地牢卡 2→1 列；Codes 3→1 列；Tier 行字母块 60px→48px
- 表格类内页：容器 `overflow-x-auto`，保证窄屏可横滑，不压缩列宽
- 目标设备：375px 宽手机为首验尺寸；LCP < 2.5s（无大图 Hero，天然达标）

## 5. SEO 与分享

- **URL 规划**（沿用关键词难度策略）：`/`、`/tier-list`、`/codes`、`/dungeons`、`/dungeons/[slug]`、`/weapons`、`/weapons/[slug]`、`/spells`、`/trading`、`/wiki`
- **Schema**：首页 `WebSite` + `SearchAction`；词条页 `Article` + `FAQPage`（TLDR 问答直接映射 FAQ schema）；地牢页 `Article` + `BreadcrumbList`
- **OG 图模板**：1200×630，深蓝底 + 门框 Logo + 页面标题（Cinzel）+ 版本徽章 + 「Verified [日期]」——保证 Reddit/Discord 截图/转发的可读性与品牌露出
- 每页 `<title>` 格式：`{主题} — Dungeon Quest Reborn | DQR.GG`；description 含「Northern Lands」「verified」等新鲜度词

## 6. 图片 / 视频 / 广告位协调

> 原则：**内容优先，媒体增色，广告让位**。首屏（Hero + TL;DR）零广告零大图，保证 LCP < 2.5s 和"3 秒得答案"。

### 6.1 图片

| 位置 | 规格 | 来源与处理 |
|---|---|---|
| 物品/法术图标（ItemChip 内） | 48×48 WebP，菱形裁切 | AI 自绘（按稀有度调色板生成），禁用官方图；emoji 占位路径 `/public/icons/[slug].webp` 留好替换 |
| 地牢横幅（地牢详情页头图 + 首页 DungeonCard 缩略） | 1200×400（详情页）/ 400×160（卡片），WebP，深色基调 | AI 自绘场景概念图（沙漠/冰雪/海盗等主题色区分），风格统一"暗夜+单主题色" |
| 攻略插图（内页步骤图） | 800×450 WebP | 自绘 UI 示意图/路线简笔画，不用游戏截图 |
| 加载策略 | — | Hero 无大图；首屏以下全部 `loading="lazy"` + 固定宽高比容器（防 CLS）；图标走 sprite 或 inline SVG |

### 6.2 视频

- **只做嵌入，不自托管**：YouTube embed（Boss 机制讲解、速通路线），必须取得作者授权或用官方允许嵌入的社区视频，视频下方标注作者与链接。
- **性能硬约束**：一律用 facade 模式（lite-youtube-embed 或自建封面+点击加载 iframe），禁止直接 iframe 自动加载；`loading="lazy"`，16:9 固定比例容器。
- **位置**：首页最多 1 个「Featured Guide」视频位（Tier 板之后）；地牢/词条内页每页最多 2 个，插在正文第 2 屏之后，绝不打断 TL;DR 和表格。

### 6.3 广告位（克制变现，先保体验）

> 受众含大量未成年玩家，**不接会跳转劫持的联盟**；建议 AdSense（需声明面向儿童）或游戏向网络（Playwire / NitroPay 类），上线初期宁可少放。

| 槽位 | 位置 | 尺寸 | 规则 |
|---|---|---|---|
| `ad-infeed-1` | 首页 Tier 板与地牢阶梯之间 | 响应式 in-feed | 样式贴近卡片但必须有「Advertisement」标注，不伪装成内容 |
| `ad-infeed-2` | 首页 Update Log 之后、页脚之前 | 响应式 | 首页仅这 2 个槽位 |
| `ad-sidebar`（桌面端内页） | 右侧栏 sticky | 300×250 | 移动端不渲染 |
| `ad-incontent`（长文内页） | 正文每 ≥3 屏 1 个，单页 ≤3 个 | in-article | 不插在表格/TL;DR 中间 |

**硬规则**：
1. 首屏 0 广告；无弹窗、无悬浮底栏 anchor、无自动播放视频广告
2. 所有广告容器预设 `min-height` + 占位骨架，防止 CLS 伤 Core Web Vitals
3. 广告位用独立 `<AdSlot>` 组件渲染，带上图占位样式（虚线框 + "Advertisement" 标签），未接广告网络时整槽不渲染、不留空白
4. 若后期数据证明广告伤害停留时长（对比 discord 社区 11 分钟中位数基准），先砍 `ad-incontent`

---

## 7. IP 合规（硬约束）

- **不使用任何官方原图**（游戏截图、官方 Logo、角色立绘）。Logo 为原创门框 + DQR 字母 + 菱形宝石（SVG 已在效果图内）；物品图标先用 emoji / Lucide 占位，路径留好便于替换为自绘图标
- 页脚固定免责声明：*Unofficial fan site. Not affiliated with or endorsed by Roblox Corporation, Voldex, or Delta Quarters OG.*
- 数据标注来源与核验日期（如 Hofund 数值存在社区分歧，需展示「数据存在来源分歧」提示，不得写死单一数值）

## 8. 上线优先级建议

- **P0（抢窗口）**：首页、`/tier-list`、`/codes`、`/dungeons/northern-lands`（含广告槽位组件但先不接网络）
- **P1**：`/dungeons` 全部 12 页、`/weapons`、`/spells`、地牢横幅 AI 配图
- **P2**：`/trading`、加点计算器（交互工具，Gemini 版遗产，二期再加，不在本次实现范围）、视频嵌入位接内容、接广告网络（观察停留时长后再放量）
